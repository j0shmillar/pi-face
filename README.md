code adapted from [ldpr](https://github.com/pandongwei/long-distance-people-recognition) 

built for use with the pi camera module

to use, first run **python get_features.py --name ‘name’** then **python3 detect.py**
